process_names:
  - name: "cookieserver"
    cmdline:
      - "cks"
      - "./bin/cks"

  - name: "cookieclient"
    cmdline:
      - "ckc"
      - "./bin/ckc"

  - name: "df_server"
    cmdline:
      - "python3 -m flask run --host 0.0.0.0"

  - name: "df_client"
    cmdline:
      - "python3 ./start_sploit.py"
